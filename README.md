# dotfiles

## New machine setup

```bash
# Clone the repository
git clone https://github.com/rokuosan/dotfiles ~/.dotfiles
# Change to the dotfiles directory
cd ~/.dotfiles
# Run the installation script
./install.sh
```
